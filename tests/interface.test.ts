import { Seller } from "../src/interface";

describe('Interface', (): void => {
    test('Interface test', (): void => {
        // cara menggunakan interface
        const seller: Seller = {
            id : 2211,
            name: "Tokopedia"
        }
    });
});